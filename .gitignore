# Go binaries
*.exe
*.exe~
*.dll
*.so
tronctl
tronctl-docs
bin/
dist/
# keys
*.pem
*.key
# tronbox keys
accounts.json
# Go test binary, build with `go test -c`
*.out
# dotenv environment variable files
.env
.env.test
# Mac system files
.DS_Store
# VS Code settings
.vscode/
# JetBrains IDEs
.idea/
*.test
# Local working notes, task files and scratch docs (never committed)
.local/
# Claude Code: local session state and personal instruction overrides
# (to share something here later, negate it, e.g. !.claude/commands/)
.claude/
CLAUDE.local.md
